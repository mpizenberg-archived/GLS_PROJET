\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{gls}[2013/10/23 Extension personnelle pour les rapports de Génie du Logiciel et des Systèmes]

\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}
\RequirePackage{graphicx}
\RequirePackage{subfig}
\RequirePackage[francais]{babel}
\RequirePackage{xcolor}
\RequirePackage{fullpage}

\RequirePackage{verbatim}
\RequirePackage{moreverb}
\RequirePackage{listingsutf8}

\RequirePackage{amsmath,amssymb}
\RequirePackage{amsthm}

\definecolor{dkgreen}{rgb}{0,0.6,0}    
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\I}{\mathbb{I}}
\newcommand{\D	}{\mathbb{D}}
\newcommand{\Pb}{( \mathfrak{P} )}
\newcommand{\p}{\mathfrak{P}}

\newtheorem{mydef}{Définition}
\newtheorem{test}{Preuve}
\newtheorem{thm}{Théorème}
\newtheorem{prop}{Proposition}

\lstset{ 
  inputencoding=utf8/latin1,
  extendedchars=true,
  language=[decorative]OCL,                  % the language of the code
  basicstyle=\footnotesize,       % the size of the fonts that are used for the code
  numbers=left,                   % where to put the line-numbers
  numberstyle=\tiny\color{gray},  % the style that is used for the line-numbers
  stepnumber=1,                   % the step between two line-numbers. If it's 1, each line 
                                  % will be numbered
  numbersep=5pt,                  % how far the line-numbers are from the code
  backgroundcolor=\color{white},      % choose the background color. You must add \usepackage{color}
  showspaces=false,               % show spaces adding particular underscores
%  showstringspaces=false,         % underline spaces within strings
%  showtabs=false,                 % show tabs within strings adding particular underscores
  frame=single,                   % adds a frame around the code
  rulecolor=\color{black},        % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. comments (green here))
  tabsize=3,                      % sets default tabsize to 3 spaces
  captionpos=b,                   % sets the caption-position to bottom
  breaklines=true,                % sets automatic line breaking
  breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
  title=\lstname,                   % show the filename of files included with \lstinputlisting;
                                  % also try caption instead of title
  keywordstyle=\color{dkgreen},          % keyword style
  commentstyle=\color{blue},       % comment style
  stringstyle=\color{mauve},         % string literal style
  escapeinside={\%*}{*)},            % if you want to add LaTeX within your code
  morekeywords={*,invariant,self,attribute,property,abstract,class,extends,...},              % if you want to add more keywords to the set
  deletekeywords={...},              % if you want to delete keywords from the given language
  literate=%
         {é}{{\'e}}1
         {à}{{\`a}}1
         {è}{{\`e}}1
         {ê}{{\^e}}1
         {î}{{\^i}}1
         {ô}{{\^o}}1
         {û}{{\^u}}1
         {ç}{{\c{c}}}1
}

\newcommand\important[1]{  
\vspace{0.2cm}   
\begin{minipage}[l]{0.5cm}
\includegraphics[width=0.5cm]{../Images/icone_important.png}
\end{minipage}
\begin{minipage}[l]{\textwidth}
\textit{#1}
\end{minipage}
\vspace{0.1cm}  
}

% Mes environnements
%\newenvironment{prop}[2]%
%	{\textcolor{red}{#1 \emph{#2}}}%
%	{}


% Mes commandes :
% \newcommand{\nomcommande}[nvariables]{macommande}
